import 'package:flutter/material.dart';

class ActivitiesDetail extends StatelessWidget {
  const ActivitiesDetail({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
